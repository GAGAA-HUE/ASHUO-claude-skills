---
name: cat-heritage-series
description: 猫咪非遗系列短视频选题大纲生成器。基于 v6.3 戏剧引擎（75-85s + 节气物候锚定 + 全部画面动态推进 + 宇宙资产联动）输出结构化单期脚本表。触发：用户提到"做一期 X"、"下一集拍 X"、"plan N 期"、"列选题"、节气名（处暑/白露/秋分等）、"宇宙联动"。
---

# 猫咪非遗系列 — 多期选题大纲生成器（v6.3）

为"猫咪为主角的非遗科普短视频系列"生成多期结构化选题大纲。基于 **v6.3 戏剧引擎** + **三猫家庭完整设定 v6.3** + **设计宪法 v6.3**。

## ⚠️ 重要：调用前必须先读 references/

为避免与已归档内容重复，**每次被调用时必须先读取**：

1. `references/drama-engine-v6.md` — **v6.3 核心宪法**（戏剧引擎 5 原理 + 节气引擎 + **v6.3 删除表情包定格** + 宇宙联动 + 段落宪法 + 自查清单）
2. `references/v6-format-conventions.md` — v6.3 输出格式约定（8 字段）
3. `references/asset-ledger.md` — 物质资产表（宇宙联动查询）
4. `references/solar-terms-calendar.md` — 节气链 + 选题池 + 物候元素库
5. `references/episode-archive-index.md` — 已有归档索引（避免重复生成）

## 适用场景

触发本技能：
- 用户说"做一期 X"、"下一集拍 X"、"规划 N 期"、"列选题"
- 用户希望基于一个非遗主题展开成完整一集的内容设计
- 用户询问"这个非遗适不适合做"
- 用户说某个节气名（"处暑做 X"、"白露拍 Y"）

不适用：
- 单条 Seedance 视频提示词生成 → 直接调用 `seedance-director` 或 `seedance-2.5-scene-director`
- 不是猫 / 不是非遗的内容 → 不要硬套本技能
- 用户已经给出完整剧本只想校验 → 直接读 v6.3 文档对照

## 输入约定

最小输入是一个**高层主题词**，例如：
- "做一期米塑"
- "plan 10 episodes covering 蜡染 / 扎染 / 蜡缬 / 夹缬 / 灰缬"
- "下一集拍陶器"
- "处暑做一期陶杯"

可选补充输入（推荐）：
- 期望期数（默认 10 期）
- 主题类别（食品 / 器具 / 染织 / 雕塑 等）
- 是否限定某个非遗级别
- 节气锚定（如"白露期"）

## 输出约定（v6.3 格式·8 个固定字段）

**每期一份结构化脚本表**，中文输出：

```
# 第 N 期 — [主题]

## 1. 基本信息（表格·含节气锚定+动作亮点+动物预算）
## 2. 工艺段（含 SOURCING / PROCESS / v6.3 5 节拍压缩）
## 3. 三猫互动节拍（v6 强化妹隐性功能 + 弟捧哏属性）
## 4. 黄毛弟行为推导（v6 强化"捧哏"属性）
## 5. 整体节奏结构（5 节拍时间轴 + v6.3 段5冲突瞬间）
## 6. 字幕卡文案汇总（≤25 字/张）
## 7. 宇宙资产联动（v6 必填·新建/复用 asset_id）
## 8. 节气锚定说明（v6 必填·氛围锚+物候呼应）
```

详细格式规范见 `references/v6-format-conventions.md`。

## v6.3 关键约束（3 条铁律）

1. **节气锚定（氛围锚·非主题锁）**——每期必挂载到具体节气，主题可与节气无强相关，但画面必须 ≥2 个元素呼应节气物候
2. **宇宙资产联动**——每期新建 / 复用 ≥1 个 asset_id，写入 `asset-ledger.md`
3. **动物配角放宽**——每期最多 1 只配角（驴/鹅/鸟/猪），仅背景或辅料位 ≤5s，不参与主线

**v6.3 关键变更**：v6 原"沙雕表情包铁律"已删除（用户反馈"画面里不要有任何的定格,所有内容全部都是动态的"）。所有画面必须连续动态推进,段 5 的"冲突定格"改为"冲突瞬间"(动态推进 2-3s)。弟闯祸本身仍是连续动态事件(闯祸类型 A-G 见 principle_12),不再有 ≥3s 逗留的表情包节拍。

## 工作流

收到输入后，**先判断输入属于哪种模式**，按对应模式执行：

### 模式 A：直接给出主题词（例："做一期米塑"）

**先检查归档索引** → 如已归档 → 直接告知用户该期编号并询问是否调整 → 否则进入 Step 1-6。

#### Step 1：验证主题是否符合 v6.3 判据
非遗题材判据 = **T1.2 可操作为主 + 半操作为辅**——师傅猫的爪子至少完成一个工艺的小闭环或半闭环动作。

#### Step 2：推导一句话知识点（≤25 字硬知识点）

#### Step 3：节气锚定（v6 必填）
- 查 `solar-terms-calendar.md` 找当前/临近节气的物候符号
- 推导 ≥2 个物候呼应画面元素（露珠 / 桂花 / 落叶 / 斜阳 / 柿子等）

#### Step 4：拆解工艺段（5 节拍压缩版）
- SOURCING 段（如有）：无集市 / 无人类 / 角色自给自足
- PROCESS 段：按 Hook / 推进 / 主爆点 / 唯美段 / 反转结尾 5 节拍压缩
- 每段以"字幕卡 + 师傅演示 + 弟捧哏 + 妹隐性反应"为单位

#### Step 5：推导三猫互动 + 动作亮点
- 招牌动作库：扑咬 / 拍头 / 推 / 抽尾巴 / 骑 / 挠
- 招牌互动升级：A 扑倒+挠 / B 推开脸+担心 / C 推不动+反差 / **D 姐替妹出头（每期强制）**
- **动作亮点（v6.3 替代表情包）**：一句话预览本期最爆笑的连续动态物理动作（如"弟偷吃莲蓉糊一脸"），不再有 ≥3s 逗留要求

#### Step 6：宇宙资产联动
- 查 `asset-ledger.md` 是否有可复用资产
- 本期新建资产 → 自动分配 A00X 编号 → 写入 `asset-ledger.md`
- 复用资产 → 标注"出现段落"

#### Step 7：输出结构化脚本表（v6.3 8 字段）

**v6.3 新增要求**：每段描述结尾必须附「衔接说明」（搬家逻辑/走路逻辑 + 具体手法 + 路标元素），详见 `references/drama-engine-v6.md` 的 `principle_15_v6_3_new`。

#### Step 8：归档
每生成一期完整脚本，立即写入 `episodes/episode-NN-[slug].md`，并在 `references/episode-archive-index.md` 添加索引条目 + 更新 `references/asset-ledger.md`。

### 模式 B：要求生成选题（例："plan 10 episodes"）

**先进入"选题生成阶段"**，输出候选列表 + 理由，等用户挑选后再进入 Step 1-8。

#### 模式 B 工作流

**B-Step 1**：确定选题边界（期数 / 节气范围 / 主题类别）
**B-Step 2**：从 `solar-terms-calendar.md` 候选池 + WebSearch 国家级非遗名录扩展
**B-Step 3**：按四维判据筛选（节气锚定 / 写实动作亮点 / 宇宙资产可建 / 工艺可操作）
**B-Step 4**：输出候选列表 + 理由
**B-Step 5**：等待用户挑选
**B-Step 6**：批量归档 → 用户确认后批量写入 episodes/ + 更新索引 + 更新 asset-ledger

## 关键约束（v6.3 强化版）

1. **节气锚定（氛围锚非主题锁）**：每期 ≥2 个物候呼应元素
2. **宇宙资产联动**：每期新建/复用 ≥1 个 asset_id
3. **动物配角放宽**：≤1 只 / ≤5s / 仅背景
4. **时长**：75-85 秒（v3/v5 是 110-130s）
5. **段落宪法**：叙事段写氛围，节拍列表写具体动作，两者互补不复读
6. **妹的隐性功能**：任务锚点 + 反应代理，**妹不主动发起**
7. **弟的捧哏属性**：每段 1 次捧哏动作补节奏
8. **招牌互动 D**：每期 ≥1 次姐替妹出头
9. **v6.3 全部画面动态推进**：无表情包定格/停帧,镜头持续跟随动作移动
10. **段 5 三段式（v6.3 动态版）**：冲突瞬间(动态 2-3s) + 克制惩罚 + 平静收束 + meta 彩蛋
11. **不要直接调用 Seedance 提示词生成**——本技能只产出结构化脚本表

## 与其他技能的关系

- **上游**：`seedance-director` / `seedance-2.5-scene-director` — 接收本技能产出的脚本表，生成 Seedance 可用提示词
- **上游（可选）**：`hailuo-h3-director` — 当需要文字精准还原（如品牌植入场景）时切换
- **平行**：`edu-video-creator` — 不调用，是另一类科普视频技能

## 参考资料

- v6.3 核心宪法：`references/drama-engine-v6.md`
- v6.3 输出格式：`references/v6-format-conventions.md`
- 物质资产表：`references/asset-ledger.md`
- 节气链 + 选题池：`references/solar-terms-calendar.md`
- 归档索引：`references/episode-archive-index.md`
- 已归档脚本表：`episodes/episode-NN-[slug].md`
- 选题清单：`episodes/pitch-list-batch-NN.md`
- v5 归档（已冻结·不读）：`references/design-spec.md` + `references/v4-format-conventions.md`

## 输出风格

- 中文为主，技术术语保留英文（如"ASMR""Seedance""meta""asset_id"）
- 表格优先，避免大段文字
- 每期单独成节，便于用户单独使用某一期
- 长度节制：单期脚本表 ≤ 250 行 / 期（v6.3 略宽于 v5）
